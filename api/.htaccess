RewriteEngine On
RewriteBase /api/

# Отдаём существующие файлы (например, /api/uploads/xxx.jpg) напрямую
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Всё остальное — в роутер
RewriteRule ^ index.php [QSA,L]
